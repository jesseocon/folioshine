<div class="navbar folio-header navbar-static-top navbar-inverse">
  <div class="navbar-inner" id="folio-header">
    <div class="container">
      <div class="navbar-content">
        <img src="https://s3-us-west-2.amazonaws.com/folioshine-icons/HeaderLogo.png">
        <ul class="nav  pull-right">
					<% if current_user %>
						<li>
							<%= link_to 'Home', albums_path %>
						</li>
	          <li>
	            <% if current_user %>
	                <%= link_to 'Sign Out', logout_path, 
	                     method: :delete, 
	                     confirmation: "Are you sure" 
	                %>
	            <% end %>
	          </li>
					<% end %>

        </ul>
      </div>
    </div>
  </div>
</div><!-- end of folio-header navbar-static-top navbar-inverse -->